import 'package:flutter/material.dart';

class Error401 extends StatelessWidget {
  const Error401({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Error 401'),
      ),
    );
  }
}
